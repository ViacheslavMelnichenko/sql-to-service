# Copy to .env and fill in. .env is gitignored and must NEVER be committed.
# Only needed to REGENERATE (pipeline/run.py). Verification (./gates/verify.sh)
# runs against committed output and needs no API key.
ANTHROPIC_API_KEY=
